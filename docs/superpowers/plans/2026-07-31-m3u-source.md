# M3U 源集成 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 支持 m3u 源导入（链接/文件），复用直播列表，源类型驱动电影/剧集 UI 显隐。

**Architecture:** Source 加 `type` 字段区分 xtream/m3u；m3u 频道复用 `live_streams` 表（加 `m3uUrl`）；`M3uPlaylistParser` 解析 `#EXTM3U`；同步/播放按源类型分支；`ActiveSourceState` 暴露当前源类型，MainScreen/HomeScreen/AllItemsScreen 按类型显隐。

**Tech Stack:** Kotlin, Room, Retrofit, OkHttp, Hilt, Jetpack Compose

**验证方式:** 无单元测试基础设施，每任务 `./gradlew compileDebugKotlin` 编译通过验证。

---

### Task 1: 数据层实体 + Migration

**Files:**
- Modify: `app/src/main/java/com/dream/wowiptv/data/local/entity/SourceEntity.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/domain/model/XtreamSource.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/data/local/entity/LiveStreamEntity.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/domain/model/LiveStream.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/data/mapper/DtoMappers.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/data/local/Migration.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/data/local/AppDatabase.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/di/DatabaseModule.kt`

- [ ] **Step 1: SourceEntity 加 type**

`SourceEntity.kt` 加字段（在 `isActive` 前）：

```kotlin
    val type: String = "xtream",
    val isActive: Boolean = false
```

- [ ] **Step 2: XtreamSource 加 type**

`XtreamSource.kt` 加字段：

```kotlin
data class XtreamSource(
    val id: Long,
    val name: String,
    val serverUrl: String,
    val port: Int,
    val username: String,
    val password: String,
    val type: String = "xtream"
)
```

- [ ] **Step 3: LiveStreamEntity 加 m3uUrl**

`LiveStreamEntity.kt` 加字段（在 `tvArchive` 后）：

```kotlin
    val tvArchive: Boolean,
    val m3uUrl: String? = null,
    val sourceId: Long
```

- [ ] **Step 4: LiveStream 加 m3uUrl**

`LiveStream.kt` 加字段：

```kotlin
data class LiveStream(
    val id: Int,
    val name: String,
    val iconUrl: String?,
    val epgChannelId: String?,
    val categoryId: Int,
    val hasArchive: Boolean,
    val m3uUrl: String? = null
)
```

- [ ] **Step 5: mapper 同步**

`DtoMappers.kt`：
- `LiveStreamDto.toDomain()` 构造 `LiveStream(...)` 末尾加 `m3uUrl = null`
- `LiveStreamEntity.toDomain()` 构造加 `m3uUrl = m3uUrl`
- `LiveStream.toEntity(sourceId)` 构造加 `m3uUrl = m3uUrl`

查找 DtoMappers.kt 中这三个函数的现有代码精确修改。

- [ ] **Step 6: Migration 7_8**

`Migration.kt` 末尾加：

```kotlin
val MIGRATION_7_8 = object : Migration(7, 8) {
    override fun migrate(db: SupportSQLiteDatabase) {
        db.execSQL("ALTER TABLE sources ADD COLUMN type TEXT NOT NULL DEFAULT 'xtream'")
        db.execSQL("ALTER TABLE live_streams ADD COLUMN m3uUrl TEXT")
    }
}
```

- [ ] **Step 7: AppDatabase version 升 8**

`AppDatabase.kt`：`version = 7` → `version = 8`

- [ ] **Step 8: DatabaseModule 注册 migration**

`DatabaseModule.kt` line 56：`.addMigrations(MIGRATION_3_4, MIGRATION_4_5, MIGRATION_5_6, MIGRATION_6_7, MIGRATION_7_8)`，加 import `com.dream.wowiptv.data.local.MIGRATION_7_8`

- [ ] **Step 9: 修复 XtreamSource 构造处**

编译会报所有 `XtreamSource(...)` 构造处因新参数（有默认值，应不报错——`type` 有默认值）。确认编译即可。

- [ ] **Step 10: 编译验证**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 11: 提交**

```bash
git add app/src/main/java/com/dream/wowiptv/data/local/entity/SourceEntity.kt app/src/main/java/com/dream/wowiptv/domain/model/XtreamSource.kt app/src/main/java/com/dream/wowiptv/data/local/entity/LiveStreamEntity.kt app/src/main/java/com/dream/wowiptv/domain/model/LiveStream.kt app/src/main/java/com/dream/wowiptv/data/mapper/DtoMappers.kt app/src/main/java/com/dream/wowiptv/data/local/Migration.kt app/src/main/java/com/dream/wowiptv/data/local/AppDatabase.kt app/src/main/java/com/dream/wowiptv/di/DatabaseModule.kt
git commit -m "feat: 数据层加源类型与 m3uUrl 字段及迁移"
```

---

### Task 2: M3uPlaylistParser

**Files:**
- Create: `app/src/main/java/com/dream/wowiptv/data/parser/M3uPlaylistParser.kt`

- [ ] **Step 1: 创建解析器**

```kotlin
package com.dream.wowiptv.data.parser

import java.net.URI

data class M3uChannel(
    val name: String,
    val logo: String?,
    val groupTitle: String?,
    val url: String
)

object M3uPlaylistParser {

    fun parse(content: String, baseUrl: String): List<M3uChannel> {
        val channels = mutableListOf<M3uChannel>()
        val lines = content.lineSequence().toList()
        var i = 0
        while (i < lines.size) {
            val line = lines[i].trim()
            if (line.startsWith("#EXTINF")) {
                val attrs = parseAttributes(line)
                val name = line.substringAfterLast(",").trim().ifBlank { attrs["tvg-name"] ?: "" }
                var url = ""
                var j = i + 1
                while (j < lines.size) {
                    val next = lines[j].trim()
                    if (next.startsWith("#EXTINF") || next.startsWith("#EXTM3U")) break
                    if (next.isNotEmpty() && !next.startsWith("#")) {
                        url = next
                        break
                    }
                    j++
                }
                if (url.isNotEmpty()) {
                    channels.add(
                        M3uChannel(
                            name = name,
                            logo = attrs["tvg-logo"]?.takeIf { it.isNotBlank() },
                            groupTitle = attrs["group-title"]?.takeIf { it.isNotBlank() },
                            url = resolveUrl(url, baseUrl)
                        )
                    )
                }
                i = j
            } else {
                i++
            }
        }
        return channels
    }

    private fun parseAttributes(extinf: String): Map<String, String> {
        val map = mutableMapOf<String, String>()
        val regex = Regex("([a-zA-Z0-9-]+)=\"([^\"]*)\"")
        regex.findAll(extinf).forEach { match ->
            map[match.groupValues[1]] = match.groupValues[2]
        }
        return map
    }

    private fun resolveUrl(url: String, baseUrl: String): String {
        if (url.startsWith("http://") || url.startsWith("https://")) return url
        return try {
            URI(baseUrl).resolve(url).toString()
        } catch (_: Exception) {
            url
        }
    }
}
```

- [ ] **Step 2: 编译验证**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/dream/wowiptv/data/parser/M3uPlaylistParser.kt
git commit -m "feat: M3U 播放列表解析器"
```

---

### Task 3: SourceRepository 支持 m3u 源

**Files:**
- Modify: `app/src/main/java/com/dream/wowiptv/domain/repository/SourceRepository.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/data/repository/SourceRepositoryImpl.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/domain/usecase/ManageSourcesUseCase.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/presentation/settings/SettingsViewModel.kt`

- [ ] **Step 1: SourceRepository.addSource 加 type**

`SourceRepository.kt`：

```kotlin
    suspend fun addSource(name: String, serverUrl: String, port: Int, username: String, password: String, type: String = "xtream"): Long
```

- [ ] **Step 2: SourceRepositoryImpl 实现 + toDomain 传 type**

`SourceRepositoryImpl.kt`：
- `addSource` 签名加 `type: String = "xtream"`，`SourceEntity(...)` 构造加 `type = type`
- `toDomain()` 构造加 `type = type`

- [ ] **Step 3: ManageSourcesUseCase.addSource 加 type**

```kotlin
    suspend fun addSource(
        name: String,
        serverUrl: String,
        port: Int,
        username: String,
        password: String,
        type: String = "xtream"
    ): Long = sourceRepository.addSource(name, serverUrl, port, username, password, type)
```

- [ ] **Step 4: SettingsViewModel.addSource 加 type**

`SettingsViewModel.addSource` 加 `type: String = "xtream"` 参数，传给 `manageSourcesUseCase.addSource(...)`，`switchSourceUseCase(newId)` 保持不变。

- [ ] **Step 5: 编译验证**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 6: 提交**

```bash
git add app/src/main/java/com/dream/wowiptv/domain/repository/SourceRepository.kt app/src/main/java/com/dream/wowiptv/data/repository/SourceRepositoryImpl.kt app/src/main/java/com/dream/wowiptv/domain/usecase/ManageSourcesUseCase.kt app/src/main/java/com/dream/wowiptv/presentation/settings/SettingsViewModel.kt
git commit -m "feat: Source 支持 m3u 类型"
```

---

### Task 4: M3uRepository 同步 + SwitchSourceUseCase 分支

**Files:**
- Create: `app/src/main/java/com/dream/wowiptv/data/repository/M3uRepository.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/domain/usecase/SwitchSourceUseCase.kt`

- [ ] **Step 1: 创建 M3uRepository**

```kotlin
package com.dream.wowiptv.data.repository

import android.content.Context
import com.dream.wowiptv.data.local.dao.LiveCategoryDao
import com.dream.wowiptv.data.local.dao.LiveStreamDao
import com.dream.wowiptv.data.local.entity.LiveCategoryEntity
import com.dream.wowiptv.data.local.entity.LiveStreamEntity
import com.dream.wowiptv.data.parser.M3uPlaylistParser
import com.dream.wowiptv.domain.model.XtreamSource
import dagger.hilt.android.qualifiers.ApplicationContext
import java.io.File
import javax.inject.Inject
import javax.inject.Singleton
import okhttp3.OkHttpClient
import okhttp3.Request

@Singleton
class M3uRepository @Inject constructor(
    @ApplicationContext private val context: Context,
    private val liveCategoryDao: LiveCategoryDao,
    private val liveStreamDao: LiveStreamDao
) {
    private val client = OkHttpClient()

    suspend fun refreshAll(source: XtreamSource) {
        val content = loadContent(source.serverUrl)
        val baseUrl = if (source.serverUrl.startsWith("file://")) "" else source.serverUrl
        val channels = M3uPlaylistParser.parse(content, baseUrl)
        if (channels.isEmpty()) throw IllegalStateException("M3U 解析无频道")

        val groups = channels.mapNotNull { it.groupTitle }.distinct()
        val catMap = groups.mapIndexed { index, name -> name to (index + 1) }.toMap()

        liveCategoryDao.deleteBySource(source.id)
        liveCategoryDao.insertAll(groups.mapIndexed { index, name ->
            LiveCategoryEntity(categoryId = index + 1, name = name, sourceId = source.id)
        })

        liveStreamDao.deleteBySource(source.id)
        liveStreamDao.insertAll(channels.mapIndexed { index, ch ->
            LiveStreamEntity(
                streamId = index + 1,
                name = ch.name,
                streamIcon = ch.logo,
                epgChannelId = null,
                categoryId = ch.groupTitle?.let { catMap[it] },
                tvArchive = false,
                m3uUrl = ch.url,
                sourceId = source.id
            )
        })
    }

    suspend fun importFileContent(content: String): String {
        // 返回 file:// 标记路径（调用方先 addSource 拿 id，再复制）
        val id = 0L
        return content
    }

    private suspend fun loadContent(serverUrl: String): String {
        if (serverUrl.startsWith("file://")) {
            val filePath = serverUrl.removePrefix("file://")
            val file = File(context.filesDir, filePath)
            return file.readText()
        }
        val request = Request.Builder().url(serverUrl).build()
        client.newCall(request).execute().use { response ->
            if (!response.isSuccessful) throw IllegalStateException("下载失败: ${response.code}")
            return response.body?.string() ?: throw IllegalStateException("响应为空")
        }
    }
}
```

注意：`loadContent` 用 OkHttp 阻塞调用，需在 `Dispatchers.IO` 上下文。`refreshAll` 的调用方（SwitchSourceUseCase）在 supervisorScope 内。为安全，`loadContent` 内用 `kotlinx.coroutines.withContext(Dispatchers.IO)` 包 OkHttp 调用。实现时加上。

- [ ] **Step 2: SwitchSourceUseCase 按 type 分支**

`SwitchSourceUseCase.kt` 注入 `m3uRepository: M3uRepository`。`invoke` 改为：

```kotlin
    suspend operator fun invoke(sourceId: Long) {
        supervisorScope {
            val active = sourceRepository.getActiveSource().first()
            sourceRepository.switchSource(sourceId)
            if (active?.type == "m3u") {
                active.let { source ->
                    launch { runCatching { m3uRepository.refreshAll(source) } }
                }
            } else {
                launch { runCatching { liveTvRepository.refreshAll() } }
                launch { runCatching { vodRepository.refreshAll() } }
                launch { runCatching { seriesRepository.refreshAll() } }
            }
            launch { refreshMemberInfo() }
        }
    }
```

注意：`switchSource` 后 `getActiveSource()` 返回新源。用 switch 前的 `active` 判断类型（若从 m3u 切到 xtream 或反之）。实际语义：传入的 `sourceId` 是目标源，`switchSource(sourceId)` 激活它。应判断**目标源**类型：用 `sourceRepository.getSources().first().find { it.id == sourceId }` 或 switch 后读取。实现时改为：先 `switchSource(sourceId)`，再 `getActiveSource().first()` 读目标源，按其 type 分支。

需要 import `kotlinx.coroutines.flow.first`。

- [ ] **Step 3: 编译验证**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: 提交**

```bash
git add app/src/main/java/com/dream/wowiptv/data/repository/M3uRepository.kt app/src/main/java/com/dream/wowiptv/domain/usecase/SwitchSourceUseCase.kt
git commit -m "feat: M3U 源同步与切换分支"
```

---

### Task 5: PlayStreamUseCase m3u 分支

**Files:**
- Modify: `app/src/main/java/com/dream/wowiptv/domain/usecase/PlayStreamUseCase.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/presentation/live/LiveViewModel.kt`

- [ ] **Step 1: PlayStreamUseCase**

`StreamType.Live` 加 `m3uUrl: String? = null`。`invoke` 开头：

```kotlin
    suspend operator fun invoke(type: StreamType): String {
        val source = sourceRepository.getActiveSource().first()
            ?: throw IllegalStateException("No active source selected")
        if (source.type == "m3u") {
            return when (type) {
                is StreamType.Live -> type.m3uUrl ?: throw IllegalStateException("M3U 频道无播放地址")
                else -> throw IllegalStateException("M3U 源不支持点播/剧集")
            }
        }
        val base = "http://${source.serverUrl}:${source.port}"
        ...
    }
```

- [ ] **Step 2: LiveViewModel.playStream 传 m3uUrl**

`LiveViewModel.playStream` 中调用：

```kotlin
                val url = playStreamUseCase(PlayStreamUseCase.StreamType.Live(stream.id, m3uUrl = stream.m3uUrl))
```

- [ ] **Step 3: 编译验证**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: 提交**

```bash
git add app/src/main/java/com/dream/wowiptv/domain/usecase/PlayStreamUseCase.kt app/src/main/java/com/dream/wowiptv/presentation/live/LiveViewModel.kt
git commit -m "feat: M3U 频道播放走 m3uUrl"
```

---

### Task 6: ActiveSourceState + SourceTypeViewModel

**Files:**
- Create: `app/src/main/java/com/dream/wowiptv/presentation/common/ActiveSourceState.kt`
- Create: `app/src/main/java/com/dream/wowiptv/presentation/common/SourceTypeViewModel.kt`

- [ ] **Step 1: ActiveSourceState**

```kotlin
package com.dream.wowiptv.presentation.common

import com.dream.wowiptv.domain.repository.SourceRepository
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.SupervisorJob
import kotlinx.coroutines.flow.SharingStarted
import kotlinx.coroutines.flow.StateFlow
import kotlinx.coroutines.flow.map
import kotlinx.coroutines.flow.stateIn
import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class ActiveSourceState @Inject constructor(
    sourceRepository: SourceRepository
) {
    val sourceType: StateFlow<String?> = sourceRepository.getActiveSource()
        .map { it?.type }
        .stateIn(
            CoroutineScope(SupervisorJob() + Dispatchers.Default),
            SharingStarted.Eagerly,
            null
        )
}
```

- [ ] **Step 2: SourceTypeViewModel**

```kotlin
package com.dream.wowiptv.presentation.common

import androidx.lifecycle.ViewModel
import dagger.hilt.android.lifecycle.HiltViewModel
import javax.inject.Inject

@HiltViewModel
class SourceTypeViewModel @Inject constructor(
    private val activeSourceState: ActiveSourceState
) : ViewModel() {
    val sourceType = activeSourceState.sourceType
}
```

- [ ] **Step 3: 编译验证**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: 提交**

```bash
git add app/src/main/java/com/dream/wowiptv/presentation/common/ActiveSourceState.kt app/src/main/java/com/dream/wowiptv/presentation/common/SourceTypeViewModel.kt
git commit -m "feat: 当前源类型共享状态"
```

---

### Task 7: UI 按源类型显隐

**Files:**
- Modify: `app/src/main/java/com/dream/wowiptv/presentation/main/MainScreen.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/presentation/home/HomeScreen.kt`
- Modify: `app/src/main/java/com/dream/wowiptv/presentation/home/AllItemsScreen.kt`

- [ ] **Step 1: MainScreen 隐藏电影/剧集 tab**

`MainScreen` 用 `hiltViewModel<SourceTypeViewModel>()`，`val sourceType by sourceTypeViewModel.sourceType.collectAsState()`。

底部导航过滤：

```kotlin
val visibleItems = if (sourceType == "m3u") {
    BottomNavItem.items.filter { it.route != BottomNavItem.Movies.route && it.route != BottomNavItem.Series.route }
} else {
    BottomNavItem.items
}
```

`BottomNavItem.items.forEach` 改 `visibleItems.forEach`。`currentRoute` 停在隐藏 tab 时（m3u 源）自动回主页：加 `LaunchedEffect(sourceType)`：

```kotlin
    LaunchedEffect(sourceType) {
        if (sourceType == "m3u" && currentRoute in listOf(BottomNavItem.Movies.route, BottomNavItem.Series.route)) {
            navController.navigate(BottomNavItem.Home.route) {
                popUpTo(navController.graph.findStartDestination().id) { saveState = true }
                launchSingleTop = true
                restoreState = true
            }
        }
    }
```

检查 `BottomNavItem` 的 `Movies`/`Series` 属性名（读 BottomNavItem.kt 确认）。

- [ ] **Step 2: HomeScreen 统计卡按类型**

`HomeScreen` 用 `hiltViewModel<SourceTypeViewModel>()`，`val sourceType by sourceTypeViewModel.sourceType.collectAsState()`。

统计卡 item 改：

```kotlin
                    item {
                        if (sourceType == "m3u") {
                            StatCard(icon = Icons.Filled.LiveTv, count = data.liveCount, label = "频道", color = Color(0xFFEF4444), modifier = Modifier.weight(1f))
                        } else {
                            Row(
                                modifier = Modifier.fillMaxWidth(),
                                horizontalArrangement = Arrangement.spacedBy(8.dp)
                            ) {
                                StatCard(icon = Icons.Filled.LiveTv, count = data.liveCount, label = "频道", color = Color(0xFFEF4444), modifier = Modifier.weight(1f))
                                StatCard(icon = Icons.Filled.Movie, count = data.movieCount, label = "电影", color = Color(0xFF818CF8), modifier = Modifier.weight(1f))
                                StatCard(icon = Icons.Filled.Tv, count = data.seriesCount, label = "剧集", color = Color(0xFFA855F7), modifier = Modifier.weight(1f))
                            }
                        }
                    }
```

- [ ] **Step 3: AllItemsScreen 隐藏 tab 行**

`AllItemsScreen` 用 `hiltViewModel<SourceTypeViewModel>()`，`val sourceType by sourceTypeViewModel.sourceType.collectAsState()`。

m3u 时：隐藏 tab 行（`item { ... tabs ... }` 整块包 `if (sourceType != "m3u")`），`items` 直接用 `data.recentLive`：

```kotlin
                if (sourceType != "m3u") {
                    item {
                        Spacer(modifier = Modifier.height(4.dp))
                        Row(horizontalArrangement = Arrangement.spacedBy(8.dp)) {
                            tabs.forEachIndexed { idx, label ->
                                FilterChip(...)
                            }
                        }
                    }
                }

                val items: List<Any> = if (sourceType == "m3u") {
                    data.recentLive
                } else {
                    when (tab) {
                        1 -> data.recentLive
                        2 -> data.recentMovies
                        3 -> data.recentSeries
                        else -> data.recentLive + data.recentMovies + data.recentSeries
                    }
                }
```

- [ ] **Step 4: 编译验证**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 5: 提交**

```bash
git add app/src/main/java/com/dream/wowiptv/presentation/main/MainScreen.kt app/src/main/java/com/dream/wowiptv/presentation/home/HomeScreen.kt app/src/main/java/com/dream/wowiptv/presentation/home/AllItemsScreen.kt
git commit -m "feat: 电影/剧集 UI 按源类型显隐"
```

---

### Task 8: 添加源表单支持 m3u

**Files:**
- Modify: `app/src/main/java/com/dream/wowiptv/presentation/settings/SourceFormScreen.kt`

- [ ] **Step 1: 类型切换 + m3u 表单**

`SourceFormScreen` 重构：`SourceFormInner` 加 `sourceType: String` state（默认 "xtream"）。类型切换用两个 FilterChip 或 SegmentedButton。m3u 类型时显示：名称 + 链接输入框 + 「选择文件」按钮 + 「导入」按钮。

文件选择用 `rememberLauncherForActivityResult(ActivityResultContracts.OpenDocument())`，MIME 过滤 `"*/*"`，返回 Uri 后用 `context.contentResolver.openInputStream(uri)` 读文本。

m3u 链接源保存：`onSaveM3u(name, url)` → `viewModel.addSource(name, url, 80, "", "", "m3u")` → 触发 switchSourceUseCase（自动下载解析）。
m3u 文件源保存：读文件内容 → 解析成功提示 → `viewModel.addSource(name, "file://", 80, "", "", "m3u")` 返回 id → 复制内容到 `filesDir/m3u/<id>.m3u` → `viewModel.updateSource(id, name, "file://m3u/<id>.m3u", 80, "", "", "m3u")`。

`SettingsViewModel` 的 `addSource`/`updateSource` 需支持传入 serverUrl 直接为完整 URL（当前 `parseServerUrl` 会拆 host/port）。m3u 需要存完整 URL 到 serverUrl。为简化：m3u 源 `serverUrl` 存完整 m3u URL，`port=80`。`parseServerUrl` 逻辑保留用于 xtream，m3u 直接传 URL 不经过 parse。

实现细节：`SourceFormScreen` 的 onSave 回调拆分 —— xtream 走现有 `parseServerUrl`，m3u 走新回调直接存 URL。

- [ ] **Step 2: 编译验证**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/dream/wowiptv/presentation/settings/SourceFormScreen.kt
git commit -m "feat: 添加源支持 m3u 链接与文件导入"
```

---

### Task 9: 全量验证

- [ ] **Step 1: 完整编译**

Run: `./gradlew compileDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 2: 文件清单确认**

- SourceEntity/XtreamSource 有 `type` 字段，MIGRATION_7_8 注册
- LiveStreamEntity/LiveStream 有 `m3uUrl`
- `M3uPlaylistParser` 存在
- `M3uRepository.refreshAll` 按 m3u 链接/本地文件加载解析写 DB
- `SwitchSourceUseCase` 按源类型分支同步
- `PlayStreamUseCase` m3u 返回 m3uUrl
- `ActiveSourceState`/`SourceTypeViewModel` 存在
- MainScreen/HomeScreen/AllItemsScreen 按类型显隐
- SourceFormScreen 支持 m3u 链接/文件导入

- [ ] **Step 3: 提交（如有遗漏）**

```bash
git status
```
若无未提交改动则跳过。
